<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="200dp"
    android:background="?attr/colorSurface">

    <!-- Full-screen container to enable vertical centering -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical|center_horizontal"
        android:orientation="vertical"
        android:padding="24dp">

        <ImageView
            android:id="@+id/imgStudent"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:layout_marginBottom="16dp"
            android:clipToOutline="true"
            android:contentDescription="Student photo"
            android:scaleType="centerCrop"
            android:src="@drawable/student5" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:fontFamily="@font/jostbold"
            android:text="Leonard Andrei Samaniego"
            android:textColor="?attr/colorOnSurface"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="50dp"
            android:fontFamily="@font/jostregular"
            android:gravity="center"
            android:text="I am a third-year student at the University of Cabuyao (PnC), currently pursuing a Bachelor of Science in Information Technology."
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="16sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnBack"
            android:layout_width="150dp"
            android:layout_height="56dp"
            android:layout_marginBottom="24dp"
            android:fontFamily="@font/jostregular"
            android:text="Back"
            android:textColor="@android:color/white"
            app:backgroundTint="@android:color/black"
            app:cornerRadius="50dp"
            app:icon="@drawable/ic_arrow_back" />

    </LinearLayout>
</ScrollView>
